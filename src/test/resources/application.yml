spring:
  mail:
    host: localhost
    port: 2525
    username: test@example.com
    password: test-password
    properties:
      mail.smtp.ssl.enable: false

app:
  email:
    from: test@example.com
    to: recipient@example.com
  ai:
    model: test-model
    max-tokens: 500
    web-search-tool-name: web_search_test

scheduler:
  cron:
    daily: "0 0 17 * * *"
